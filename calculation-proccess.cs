//servicio de proceso el cual tiene la responsabilidad de procesar informacion relacionada con los clientes
//y entregar respuestas significativas para que otros servicios de clientes las formateen y las entreguen 

function TaxesData CalulateTaxesData(DataClient dataClient)
{
    return new TaxesData();
}


function TaxesData CalulateTaxesData2(DataClient dataClient)
{
    return new TaxesData();
}

//comentario para que aparezca el merge relacionado funcion 3
function TaxesData CalulateTaxesData3(DataClient dataClient)
{
    return new TaxesData();
}

//comentario en clientTwo